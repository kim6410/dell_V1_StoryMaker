# Dell V1 체험 연구실 브리지 수정 전 백업 및 Git 기준점

- 작업 일시: 2026-07-27 01:05 KST
- 작업 목적: 체험 연구실의 오래된 작업 ID 채택 및 MP4 이중 업로드 수정 전 운영 기준본 보존
- 코드 수정: 없음
- 삭제/이동: 없음

## 백업

`/home/bourne/StoryMaker_1/Backup/V1_WORKING_20260727_010523_체험연구실_브리지_수정전`

백업 파일:

- `index.html`
- `v1-podcast-frontend-recovery.js`
- `v1-browser-mp4-save-bridge.js`

## Git 기준점

현재 운영 중이지만 Git 미추적 상태였던 다음 파일을 내용 변경 없이 기준 파일로 등록한다.

- `storymaker-web/backend/app/static/v1/v1-podcast-frontend-recovery.js`
- `storymaker-web/backend/app/static/v1/v1-browser-mp4-save-bridge.js`

## 확인 결과

- V1/V2 체험 연구실 React 컴포넌트는 동일함
- V1 브라우저 TTS, WAV, MP3, SRT, WebCodecs MP4 생성 자체는 정상임
- V1 전용 브리지에서 최신 DB 작업 자동 채택과 MP4 중복 저장이 확인됨
- 수정 대상은 위 브리지 2개로 제한 가능함

## 다음 작업

1. 체험 연구실 경로에서 최신 DB 작업 감시 및 자동 팟캐스트 실행 차단
2. 명시적인 현재 `mob-*` 작업이 없는 체험 연구실 MP4 자동 저장 차단
3. 동일 MP4의 직접 업로드와 DOM 스캔 업로드 중복 제거
4. JavaScript 문법, HTTP 200, Git diff 및 실제 브라우저 흐름 검증

## 수정 금지 범위

- React 활성 번들
- V2
- Beta
- TTS/MP4 코어 엔진
- 백엔드 API, Worker, Queue, DB

## 롤백

백업 폴더의 파일을 동일한 V1 정적 파일 경로에 복원하고 V1 `index.html` 캐시 버전을 원래 값으로 되돌린다.
