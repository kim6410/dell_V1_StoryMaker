# Dell V1 체험 연구실 진행 표시 및 BGM 경로 수정

## 작업 일시

- 2026-07-27

## 작업 목적

- 영상 만들기 클릭 직후 실제 TTS 진행률이 시작되기 전에도 준비 상태가 움직이도록 표시
- 영상 만들기 버튼, 진행바, 상태 문구를 상세 설정 위로 이동
- V1 음악 manifest가 반환하는 `/api` 파일 주소로 인해 일부 BGM이 404로 중단되는 문제 차단

## 수정 전 백업

- `/home/bourne/StoryMaker_1/Backup/V1_WORKING_20260727_012936_체험연구실_즉시진행_BGM경로_수정전`

## 수정 파일

- `storymaker-web/backend/app/static/v1/v1-podcast-frontend-recovery.js`
- `storymaker-web/backend/app/static/v1/index.html`

## 적용 내용

- 체험 연구실의 영상 만들기 버튼 클릭 시 준비 진행 표시를 2%부터 최대 18%까지 점진적으로 이동
- 실제 진행률이 28% 이상이면 준비 표시를 제거하고 React 실제 진행률로 인계
- 준비율 상한에 도달해도 흐르는 애니메이션을 유지해 멈춘 인상을 줄임
- 영상 만들기 버튼, 진행바, 상태 문구를 상세 설정 그룹 바로 위로 재배치
- 체험 연구실에서 `/api/podcast/public/music/file/` 요청만 `/v1-api/podcast/public/music/file/`로 교정
- 브리지 캐시 버전을 `20260727-experience-progress-bgm-1`로 변경

## 원인 확인

- 첫 실행은 ONNX 음성 모델 로드와 WebGPU 세션 및 셰이더 준비가 필요함
- 같은 탭의 두 번째 실행은 준비된 세션을 메모리에서 재사용하므로 빨라짐
- 음악 manifest 61곡 중 8곡은 `/api` 경로에서 404였으나 `/v1-api` 경로에서는 61곡 모두 정상

## 검사 및 테스트

- `node --check`: 통과
- `git diff --check`: 통과
- 공개 V1: HTTP 200
- V1 복구 브리지: HTTP 200
- V1 음악 manifest: HTTP 200
- 문제 BGM `alexgrohl-inspiring-sport-rock-trailer-478796.mp3`: `/v1-api` HTTP 206
- JavaScript 파일 UTF-8 확인
- 브라우저 강력 새로고침 완료
- 버튼 클릭 0.7초 후 실제 1%, 준비 표시 2.88% 확인
- 실제 진행률 28% 이후 준비 표시 자동 제거 확인
- 음악 URL `/api`에서 `/v1-api`로 교정되는 브라우저 로그 확인
- 브라우저 WebGPU TTS, PCM 믹싱, WebCodecs MP4 생성 완료
- 생성 영상 길이 37.5초 확인

## 생성 및 삭제

- 생성: 이 업무일지 1개
- 삭제: 없음
- 비활성화: 없음

## 미확인 및 남은 위험

- 첫 방문의 모델 준비 시간은 사용자 GPU, 브라우저 캐시 및 네트워크 속도에 따라 달라짐
- 준비 진행 표시는 실제 모델 바이트 진행률이 아니라 사용자에게 준비 중임을 알리는 완만한 표시임
- React 활성 번들을 직접 수정하지 않았으므로 향후 DOM 구조가 바뀌면 브리지의 위치 탐색 조건을 함께 조정해야 함

## 절대 수정하지 않은 범위

- React 활성 번들
- Dell V2
- StoryMaker Beta
- API, Worker, Queue, DB

## 롤백

1. 백업 폴더의 `v1-podcast-frontend-recovery.js`와 `index.html`을 원래 경로에 복원
2. 공개 V1과 브리지 HTTP 200 확인
3. 브라우저 강력 새로고침
