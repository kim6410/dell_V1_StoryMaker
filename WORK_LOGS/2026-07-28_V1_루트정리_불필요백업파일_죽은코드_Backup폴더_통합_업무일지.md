# V1 루트 정리 — 불필요 백업파일·죽은 코드 Backup 폴더 통합 업무일지

- 작업 일시: 2026-07-28 22:52 ~ 22:58 KST
- 작업 루트: `/home/bourne/StoryMaker_1`
- 작업 성격: **파일 이동만 수행(삭제 없음)**. 00_READ_FIRST 10번(삭제·이동 원칙)에 따라 대상 목록을 먼저 보고하고 사용자 승인을 받은 뒤 개별 경로 단위로 이동했다. 와일드카드 삭제·이동은 사용하지 않았다.
- 이동 후 종료 시점 확인: DB 변경 없음, 서비스 재시작 없음, 코드 로직 변경 없음.

## 1. 작업 목적

루트 디렉토리와 하위 경로에 흩어져 있던 대형 백업 폴더·구버전 DB 스냅샷·미사용 죽은 코드 파일을 공식 백업 위치(`/home/bourne/StoryMaker_1/Backup`)로 통합 정리했다.

## 2. 사전 조사 및 승인 절차

1. `find`/`du`로 루트 전체를 스캔해 대형 파일·백업 패턴 파일 후보를 식별했다.
2. 각 후보의 실제 참조 여부(예: `api/` 죽은 코드 파일이 `main.py`나 다른 파일에서 import되는지)를 grep으로 확인했다.
3. 후보 목록(7개 항목, 소유자·크기·용도 포함)을 사용자에게 먼저 보고하고 승인을 받았다.
4. root 소유(`root:root`) 항목은 `bourne` 계정으로 쓰기 권한이 없어(`touch` 테스트 시 Permission denied), 비밀번호 없는 sudo도 불가능함을 확인하고 사용자에게 직접 실행할 `sudo mv` 명령을 개별 경로로 제공했다.
5. 사용자가 직접 4개 항목의 sudo 이동과 `chown -R bourne:bourne`을 실행했고, 결과를 다시 보고해왔다.

## 3. 이동 대상 및 결과

목적지 공통 경로: `/home/bourne/StoryMaker_1/Backup/CLEANUP_20260728_225248_root_junk_consolidation/`

| # | 원본 경로 | 이동 후 경로 | 크기 | 처리 주체 | 원본 소유자 |
|---|---|---|---|---|---|
| 1 | `database/backups/content_intelligence.db.backup_v2_content_intelligence_20260719_011119` | `.../database_backups_20260719/` | 36KB | AI(직접) | bourne |
| 2 | `database/backups/storymaker.db.backup_v2_mode_tables_20260719_011119` | `.../database_backups_20260719/` | 1004KB | AI(직접) | bourne |
| 3 | `storymaker-v1-app.zip` (루트, 용도 불명) | `.../storymaker-v1-app.zip` | 5.6KB | AI(직접) | bourne |
| 4 | `storymaker-web/backend/app/api/mobile_one_shot - 07201021.py` (미사용 죽은 코드, import 0건 확인) | `.../dead_code_api_files/` | 131KB | AI(직접) | bourne |
| 5 | `storymaker-web/backend/app/api/slideshow - 07201021.py` (미사용 죽은 코드, import 0건 확인) | `.../dead_code_api_files/` | 46KB | AI(직접) | bourne |
| 6 | `Backups/`(공식 `Backup/`와 다른 비표준 폴더, `pre_smart_copy_20260727_094222/` 432MB + DB 안전백업 2개 24MB 포함) | `.../Backups_root_owned_20260727_28/` | 456MB | **사용자 직접(sudo)** | root |
| 7 | `.supertonic_data_stage_20260725/`(빈 스테이징 폴더 흔적) | `.../supertonic_data_stage_20260725/` | 8KB | **사용자 직접(sudo)** | root |
| 8 | `.slideshow_stage_20260725/`(빈 스테이징 폴더 흔적) | `.../slideshow_stage_20260725/` | 8KB | **사용자 직접(sudo)** | root |

이동 후 사용자가 `sudo chown -R bourne:bourne`으로 6~8번의 소유자를 `bourne`으로 통일했다.

## 4. 검증 결과

### 4.1 해시 대조 (1~5번, AI가 이동 전후 직접 계산)

이동 전/후 SHA-256이 5개 파일 모두 정확히 일치함을 확인했다(값은 이 문서에 기록하지 않고 일치 여부만 기록).

### 4.2 해시 대조 (6번, 자체 포함된 체크섬 파일 사용)

`pre_smart_copy_20260727_094222/` 폴더 안에 원본 생성 시점(07-27)에 함께 만들어진 `SHA256SUMS.txt`가 포함되어 있었다. 이 파일의 경로가 이동 전 절대경로를 가리키고 있어 `sha256sum -c`로 직접 검증할 수는 없었으나, 파일 3개(`source_snapshot.tar.gz`, `git_status.txt`, `tracked_changes.patch`)를 새 위치에서 재계산해 기록된 해시값과 완전히 일치함을 확인했다.

### 4.3 원본 경로 소멸 확인

`database/backups/`, 루트의 `storymaker-v1-app.zip`, `api/` 안의 죽은 코드 2개, `Backups/`, `.supertonic_data_stage_20260725/`, `.slideshow_stage_20260725/` 모두 원래 위치에서 완전히 사라졌음을 `ls` 오류(No such file or directory)로 확인했다.

### 4.4 서비스·DB 영향 확인

- 이동된 죽은 코드 2개는 사전에 `main.py`와 `api/` 전체에서 import되지 않음을 grep으로 확인한 뒤 이동했다.
- V1 서비스, DB, 다른 정적 자산은 이번 이동으로 건드리지 않았다.

## 5. 용량 변화

**핵심**: 이번 작업은 삭제가 아니라 같은 디스크(`/dev/nvme0n1p2`) 내에서의 재배치이므로 디스크 여유 공간은 변하지 않는다.

| 항목 | 이동 전 | 이동 후 |
|---|---|---|
| 디스크 전체(`/dev/nvme0n1p2`) | 915G 중 사용 234G, 여유 635G(27%) | **동일**: 915G 중 사용 234G, 여유 635G(27%) |
| `/home/bourne/StoryMaker_1` 총량 | 5.7G | **동일**: 5.7G |
| `Backup/`(공식) | 1.2G | **1.7G**(+457M, 이번에 통합된 항목만큼 증가) |
| 루트의 `Backups/`(비표준) | 456M | **0**(완전히 이동되어 사라짐) |
| 루트의 스테이징 폴더 2개 | 각 8K | **0** |
| `api/` 안의 죽은 코드 파일 | 177K | **0**(더 이상 실제 코드와 섞여 있지 않음) |

## 6. 정상 확인 항목

- 8개 항목 전부 원본 소멸 + 새 위치 존재 확인
- 5개 항목(AI 직접 처리분) SHA-256 이동 전후 일치
- 3개 항목(사용자 sudo 처리분) 내장 체크섬 파일 기준 일치
- root 소유 항목의 소유자가 `bourne`으로 정상 통일됨
- 죽은 코드 파일 제거가 실제 서비스 코드에 영향 없음(사전 import 검사로 확인)

## 7. 미확인 항목

- `pre_smart_copy_20260727_094222/`(432MB)가 현재도 필요한 백업인지, 아니면 이후 다른 백업으로 대체되어 삭제 가능한지는 확인하지 않았다. 사용자가 별도로 요청하면 그 시점의 실제 삭제 필요성을 재조사할 예정이다.
- 루트에 남아있는 실행 스크립트(`patch_beta.py`, `rollback_storymaker_v1_3h.sh`, `proxy_v1_5174.py`, `apply_v1_isolation_compose.sh`, `collect_backups_to_Backup.sh`)는 00_READ_FIRST의 "기존 실행 파일 임의 이동 금지" 원칙에 따라 이번 정리 대상에서 명시적으로 제외했다.

## 8. 남은 문제

없음(이번 작업 범위 내에서는 데이터 손실이나 서비스 영향이 발견되지 않음).

## 9. 다음 작업 순서

- 필요 시 `pre_smart_copy_20260727_094222/` 스냅샷의 대체 여부를 확인한 뒤 삭제 여부 논의.
- 루트 실행 스크립트들의 실제 사용 여부를 별도로 조사할지는 사용자 결정 대기.

## 10. 절대 수정 금지 범위

Dell 운영 V2, 공용 StoryMaker, 공용 Supertonic3(`/home/bourne/Supertonic3`), 포트 7788, 보호 번들 `BrowserMp4TestPage-CmPBgwv3.js` — 이번 작업에서 열람·이동하지 않았다.

## 11. 롤백 방법

이동된 모든 항목은 삭제되지 않고 `Backup/CLEANUP_20260728_225248_root_junk_consolidation/` 하위에 원형 그대로 보존되어 있다. 원복이 필요하면 해당 서브폴더를 원래 경로로 다시 이동하면 된다(root 소유였던 3개 항목은 원복 시에도 `sudo mv` 필요).
