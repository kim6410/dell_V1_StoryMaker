# V1 제미나이 프롬프트 최신 날씨 캐시 DB 연결 업무일지

작성일: 2026-07-28

## 작업 목적

기존 `weather_daily_summaries` 중심의 프롬프트 날씨 조회를 요청형 최신 캐시 DB인 `weather_cache.db`와 직접 연결하고, 실제 제미나이 전달용 최종 프롬프트에 최신 날씨와 기온이 포함되는지 검증한다.

## 사전 백업

백업 폴더:

`/home/bourne/StoryMaker_1/Backup/V1_WEATHER_PROMPT_20260728_125558_before`

백업 파일:

- `prompt_builder.py`
- `weather_cache_service.py`
- `SHA256SUMS.txt`

수정 전 SHA-256:

- `prompt_builder.py`: `67d8947222da977573426e28ea3b2a83bd39f1ca4bd8b6287de649b393965dcf`
- `weather_cache_service.py`: `de7c6c2354634c401c12d733fbe2e6725c1bf3da4c7d9fd997d2e81c863c7205`

## 실제 변경 파일

`/home/bourne/StoryMaker_1/storymaker-web/backend/app/core/prompt_builder.py`

`weather_cache_service.py`는 사전 백업만 수행했고 이번 연결 작업에서는 수정하지 않았다.

## 변경 내용

`fetch_weather_context_for_prompt()`의 조회 흐름을 다음처럼 변경했다.

1. `fetch_weather_and_temp(region)` 호출
2. 내부 `get_or_fetch_weather()`를 통해 `weather_cache.db`의 60분 이내 최신 캐시 우선 사용
3. 캐시가 없거나 만료되면 요청형 외부 조회 후 최신 한 건 저장
4. 외부 조회 실패 시 직전 캐시 사용
5. 모든 조회 실패 시 안전한 기본 안내 문장 반환
6. 반환된 날씨와 기온을 제미나이 프롬프트용 자연어 문장으로 구성

기존의 강제 예외 코드 `raise RuntimeError("weather_daily_summaries miss")`는 제거했다.

## 검증

### 문법 검사

- 호스트 Python `py_compile`: PASS
- 컨테이너 Python `py_compile`: PASS

### 컨테이너

- 대상: `storymaker-v1-backend`
- 재시작: 정상
- 애플리케이션 startup: 정상

### 실제 프롬프트 생성 테스트

테스트 지역: `서울`

캐시 원본:

- 날씨: `대체로 맑음`
- 기온: `30.6`
- fetched_at: `2026-07-28T12:45:08+09:00`
- 테스트 전 access_count: `2`

실제 `build_prompt_markdown()` 실행 결과:

- 최종 프롬프트 길이: `9,959`자
- 최신 날씨 문장 포함: PASS
- 날씨 상태 `대체로 맑음` 포함: PASS
- 기온 `30.6℃` 포함: PASS

최종 프롬프트에 확인된 실제 문장:

`- 오늘 날씨: 7월 28일 한여름, 화요일 점심 무렵입니다. 오늘 서울의 날씨는 대체로 맑음입니다. 현재 기온은 30.6℃입니다. 날씨를 길게 설명하지 말고 오늘의 공기와 현장 분위기에 자연스럽게 반영해 주세요.`

테스트 후 access_count: `4`

`fetched_at`은 변하지 않았고 access_count만 증가했으므로 외부 날씨 API를 다시 호출하지 않고 기존 최신 캐시를 재사용한 것으로 확인했다.

## 최종 판정

STATUS=PASS

- 최신 요청형 날씨 캐시 DB와 제미나이 프롬프트 연결 정상
- 실제 최종 프롬프트에 날씨·기온 포함 정상
- 캐시 재사용 정상
- 외부 API 중복 호출 방지 정상
- V1 백엔드 기동 정상

## 롤백

문제가 발생하면 아래 백업 파일을 원래 위치로 복원한다.

`/home/bourne/StoryMaker_1/Backup/V1_WEATHER_PROMPT_20260728_125558_before/prompt_builder.py`

복원 대상:

`/home/bourne/StoryMaker_1/storymaker-web/backend/app/core/prompt_builder.py`
